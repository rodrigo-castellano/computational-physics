            program ejer3
       real x,
       integer i,j,k
       x=0.0

       write(*,*)'escribe el valor maximo a mostrar'
       read(*,*)i
       do 8 j=1,i
       write(*,*)i,' '
       continue

       pause
       stop
       end
